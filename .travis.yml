language: node_js
node_js:
  - "4"
  - "5"
  - "6"
  - "7"
  - "8"

before_install:
  - if [[ `npm -v` < 3* ]]; then npm i -g npm@latest; fi

before_script:
  - node --version
  - npm --version

script:
  - npm test

after_script:
  - npm run cover

matrix:
  fast_finish: true
